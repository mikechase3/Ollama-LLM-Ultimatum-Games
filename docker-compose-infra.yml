# DEPRECATED FILE
#
# Use docker-compose.yml instead. It includes:
# - ollama (LLM server)
# - webui (OpenWebUI)
# - dashboard (Streamlit app)
#
# Quick infra-only start from the main file:
#   docker compose up -d ollama webui
#
# Or full stack:
#   docker compose up -d